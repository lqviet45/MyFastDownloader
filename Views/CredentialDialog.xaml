<!--
    Credential Add/Edit Dialog
    Modern WPF design with validation and security
-->
<Window x:Class="MyFastDownloader.App.Views.CredentialDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Credential Authentication"
        Height="550"
        Width="600"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0F172A"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#1E293B"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#60A5FA"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#10B981"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8"/>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#475569"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#64748B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Border Background="#0F172A"
            CornerRadius="16"
            BorderBrush="#334155"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30"
                            ShadowDepth="8"
                            Color="Black"
                            Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#1E293B"
                    CornerRadius="16,16,0,0"
                    Padding="20,16">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Border Background="White"
                                CornerRadius="8"
                                Width="32"
                                Height="32"
                                Margin="0,0,12,0">
                            <TextBlock Text="🔐"
                                     FontSize="18"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="TitleTextBlock"
                                     Text="Thêm Credential Mới"
                                     FontSize="18"
                                     FontWeight="Bold"
                                     Foreground="White"/>
                            <TextBlock Text="Nhập thông tin xác thực"
                                     FontSize="12"
                                     Foreground="#94A3B8"
                                     Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Button HorizontalAlignment="Right"
                            Width="32"
                            Height="32"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CancelButton_Click">
                        <TextBlock Text="✕"
                                 FontSize="16"
                                 Foreground="White"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Padding="20">
                <StackPanel>
                    <!-- Name -->
                    <TextBlock Text="Tên (tùy chọn):"
                             Foreground="#94A3B8"
                             FontSize="13"
                             Margin="0,0,0,6"/>
                    <TextBox x:Name="NameTextBox"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,16"/>

                    <!-- Domain -->
                    <TextBlock Foreground="#94A3B8"
                             FontSize="13"
                             Margin="0,0,0,6">
                        <Run Text="Domain/URL: "/>
                        <Run Text="*" Foreground="#EF4444"/>
                    </TextBlock>
                    <TextBox x:Name="DomainTextBox"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,6"/>
                    <TextBlock Text="Ví dụ: example.com, *.github.com, https://api.service.com"
                             Foreground="#64748B"
                             FontSize="11"
                             Margin="0,0,0,16"/>

                    <!-- Authentication Mode -->
                    <TextBlock Foreground="#94A3B8"
                             FontSize="13"
                             Margin="0,0,0,6">
                        <Run Text="Loại xác thực: "/>
                        <Run Text="*" Foreground="#EF4444"/>
                    </TextBlock>
                    <ComboBox x:Name="AuthModeComboBox"
                              Style="{StaticResource ModernComboBox}"
                              SelectionChanged="AuthModeComboBox_SelectionChanged"
                              Margin="0,0,0,16">
                        <ComboBoxItem Content="Basic Authentication" Tag="Basic"/>
                        <ComboBoxItem Content="Digest Authentication" Tag="Digest"/>
                        <ComboBoxItem Content="NTLM Authentication" Tag="NTLM"/>
                        <ComboBoxItem Content="Bearer Token (OAuth)" Tag="Bearer"/>
                    </ComboBox>

                    <!-- Username/Password Section -->
                    <StackPanel x:Name="UsernamePasswordPanel">
                        <!-- Username -->
                        <TextBlock Foreground="#94A3B8"
                                 FontSize="13"
                                 Margin="0,0,0,6">
                            <Run Text="Username: "/>
                            <Run Text="*" Foreground="#EF4444"/>
                        </TextBlock>
                        <TextBox x:Name="UsernameTextBox"
                                 Style="{StaticResource ModernTextBox}"
                                 Margin="0,0,0,16"/>

                        <!-- Password -->
                        <TextBlock Foreground="#94A3B8"
                                 FontSize="13"
                                 Margin="0,0,0,6">
                            <Run Text="Password: "/>
                            <Run Text="*" Foreground="#EF4444"/>
                        </TextBlock>
                        <PasswordBox x:Name="PasswordBox"
                                     Style="{StaticResource ModernPasswordBox}"
                                     Margin="0,0,0,16"/>
                    </StackPanel>

                    <!-- Bearer Token Section -->
                    <StackPanel x:Name="BearerTokenPanel" Visibility="Collapsed">
                        <TextBlock Foreground="#94A3B8"
                                 FontSize="13"
                                 Margin="0,0,0,6">
                            <Run Text="Bearer Token: "/>
                            <Run Text="*" Foreground="#EF4444"/>
                        </TextBlock>
                        <TextBox x:Name="BearerTokenTextBox"
                                 Style="{StaticResource ModernTextBox}"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 Height="80"
                                 Margin="0,0,0,16"/>
                    </StackPanel>

                    <!-- Test URL -->
                    <TextBlock Text="Test URL (tùy chọn):"
                             Foreground="#94A3B8"
                             FontSize="13"
                             Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="TestUrlTextBox"
                                 Grid.Column="0"
                                 Style="{StaticResource ModernTextBox}"/>
                        
                        <Button Grid.Column="1"
                                Content="🔍 Test"
                                Style="{StaticResource ModernButton}"
                                Margin="10,0,0,0"
                                Click="TestButton_Click"/>
                    </Grid>

                    <!-- Notes -->
                    <TextBlock Text="Ghi chú:"
                             Foreground="#94A3B8"
                             FontSize="13"
                             Margin="0,0,0,6"/>
                    <TextBox x:Name="NotesTextBox"
                             Style="{StaticResource ModernTextBox}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Height="60"
                             Margin="0,0,0,16"/>

                    <!-- Active Checkbox -->
                    <CheckBox x:Name="IsActiveCheckBox"
                              Content="Kích hoạt credential này"
                              Foreground="White"
                              FontSize="13"
                              IsChecked="True"
                              Margin="0,0,0,0"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2"
                    Background="#1E293B"
                    BorderBrush="#334155"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,16,16"
                    Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="StatusTextBlock"
                             Grid.Column="0"
                             Text=""
                             FontSize="12"
                             Foreground="#94A3B8"
                             VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="✓ Lưu"
                            Style="{StaticResource ModernButton}"
                            Background="#10B981"
                            MinWidth="100"
                            Click="SaveButton_Click"/>

                    <Button Grid.Column="2"
                            Content="✗ Hủy"
                            Style="{StaticResource ModernButton}"
                            MinWidth="100"
                            Margin="10,0,0,0"
                            Click="CancelButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
