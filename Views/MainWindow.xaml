<Window x:Class="MyFastDownloader.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MyFastDownloader" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#FF0F172A">
    
    <Window.Resources>
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1E40AF" Offset="0"/>
            <GradientStop Color="#3730A3" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="GreenButtonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#10B981" Offset="0"/>
            <GradientStop Color="#059669" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="BlueButtonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366F1" Offset="0"/>
            <GradientStop Color="#4F46E5" Offset="1"/>
        </LinearGradientBrush>
        
        <LinearGradientBrush x:Key="OrangeButtonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F59E0B" Offset="0"/>
            <GradientStop Color="#D97706" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#60A5FA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="#3B82F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Border Name="PART_Track" CornerRadius="4">
                                <Border Name="PART_Indicator" 
                                        HorizontalAlignment="Left" 
                                        CornerRadius="4">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#3B82F6" Offset="0"/>
                                            <GradientStop Color="#1D4ED8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource HeaderGradient}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- App Title -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                    <Border Background="White" CornerRadius="8" Width="32" Height="32" Margin="0,0,12,0">
                        <Viewbox Width="20" Height="20">
                            <Canvas Width="24" Height="24">
                                <Path Fill="#1E40AF" Data="M12,2 L2,7 L12,12 L22,7 Z"/>
                                <Path Stroke="#1E40AF" StrokeThickness="2" Data="M2,17 L12,22 L22,17"/>
                                <Path Stroke="#1E40AF" StrokeThickness="2" Data="M2,12 L12,17 L22,12"/>
                            </Canvas>
                        </Viewbox>
                    </Border>
                    <TextBlock Text="MyFastDownloader" FontSize="24" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- URL Input Section -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="UrlTextBox" 
                             Grid.Column="0"
                             Style="{StaticResource ModernTextBox}"
                             Text="{Binding DownloadUrl, UpdateSourceTrigger=PropertyChanged}"
                             KeyDown="UrlTextBox_KeyDown">
                    </TextBox>

                    <Button x:Name="AddButton" 
                            Grid.Column="1" 
                            Style="{StaticResource ModernButton}"
                            Background="{StaticResource GreenButtonGradient}"
                            Margin="12,0,0,0"
                            Padding="20,12"
                            Click="AddButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Thêm" FontSize="14"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="1" Margin="20" Background="#1E293B" CornerRadius="16" BorderBrush="#334155" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Downloads Header -->
                <Grid Grid.Row="0" Margin="20,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Danh sách tải xuống" FontSize="18" FontWeight="SemiBold" Foreground="#E2E8F0"/>
                    <TextBlock Grid.Column="1" Text="{Binding DownloadStats, Mode=OneWay}" FontSize="14" Foreground="#94A3B8"/>
                </Grid>

                <!-- Downloads List -->
                <Border Grid.Row="1" BorderBrush="#475569" BorderThickness="0,1,0,0">
                    <Grid>
                        <ListView x:Name="DownloadsListView" 
                                  ItemsSource="{Binding Downloads}"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListView.Resources>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#334155"/>
                                    <Setter Property="Padding" Value="20,16"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1A3B82F6"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="150"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="140"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- File Info -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding FileName, Mode=OneWay}" FontWeight="Medium" FontSize="14" Foreground="#E2E8F0" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding SizeText, Mode=OneWay}" FontSize="12" Foreground="#94A3B8" Margin="0,4,0,0"/>
                                        </StackPanel>

                                        <!-- Status -->
                                        <Border Grid.Column="1" 
                                                BorderBrush="{Binding StatusColor, Mode=OneWay}"
                                                BorderThickness="1"
                                                CornerRadius="20" 
                                                Padding="12,6"
                                                HorizontalAlignment="Center"
                                                Background="#1A000000">
                                            <TextBlock Text="{Binding StatusText, Mode=OneWay}" 
                                                       Foreground="{Binding StatusColor, Mode=OneWay}" 
                                                       FontSize="12" 
                                                       FontWeight="Medium"
                                                       TextAlignment="Center"/>
                                        </Border>

                                        <!-- Progress -->
                                        <StackPanel Grid.Column="2">
                                            <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" 
                                                         Maximum="100" 
                                                         Style="{StaticResource ModernProgressBar}"/>
                                            <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:F0}%, Mode=OneWay}" 
                                                       FontSize="12" 
                                                       Foreground="#94A3B8" 
                                                       TextAlignment="Center"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>

                                        <!-- Speed -->
                                        <TextBlock Grid.Column="3" 
                                                   Text="{Binding SpeedText, Mode=OneWay}" 
                                                   FontSize="14" 
                                                   FontWeight="Medium"
                                                   Foreground="#60A5FA"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>

                                        <!-- Actions -->
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Center">
                                            <!-- Play/Pause Button -->
                                            <Button Padding="10"
                                                    Margin="0,0,8,0"
                                                    Click="ActionButton_Click"
                                                    Tag="{Binding}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Background" Value="{StaticResource GreenButtonGradient}"/>
                                                        <Setter Property="Content" Value="▶"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Downloading">
                                                                <Setter Property="Background" Value="{StaticResource OrangeButtonGradient}"/>
                                                                <Setter Property="Content" Value="⏸"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Paused">
                                                                <Setter Property="Background" Value="{StaticResource GreenButtonGradient}"/>
                                                                <Setter Property="Content" Value="▶"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                                                <Setter Property="Background" Value="{StaticResource GreenButtonGradient}"/>
                                                                <Setter Property="Content" Value="↻"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Queued">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            
                                            <!-- Open Folder Button -->
                                            <Button Padding="10"
                                                    Click="OpenButton_Click"
                                                    Tag="{Binding}">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                                        <Setter Property="Background" Value="{StaticResource BlueButtonGradient}"/>
                                                        <Setter Property="Content" Value="📂"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <!-- Empty State -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding EmptyStateVisibility, Mode=OneWay}">
                            <TextBlock Text="📥" FontSize="64" Opacity="0.5" HorizontalAlignment="Center"/>
                            <TextBlock Text="Chưa có tải xuống nào" FontSize="16" Foreground="#64748B" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                            <TextBlock Text="Nhập URL ở trên để bắt đầu tải xuống" FontSize="12" Foreground="#64748B" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="0,1,0,0" Padding="20,16">
            <Grid>
                <TextBlock Text="Server đang chạy tại http://localhost:4153 • Sử dụng /add?url=YOUR_URL để thêm download" 
                           FontSize="14" 
                           Foreground="#94A3B8"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{Binding StatusMessage, Mode=OneWay}" 
                           FontSize="12" 
                           Foreground="#60A5FA"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>