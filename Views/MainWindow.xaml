<!--
    MyFastDownloader - Improved Modern UI (OPTIMIZED SIZE)
    Compact, professional design with smaller components
-->
<Window x:Class="MyFastDownloader.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MyFastDownloader - Modern Download Manager" 
        Height="650" 
        Width="1100"
        MinHeight="550"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#FF0F172A"
        AllowsTransparency="False"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- Modern Color Palette -->
        <Color x:Key="PrimaryDark">#0F172A</Color>
        <Color x:Key="SecondaryDark">#1E293B</Color>
        <Color x:Key="AccentDark">#334155</Color>
        <Color x:Key="AccentLight">#475569</Color>
        <Color x:Key="TextPrimary">#E2E8F0</Color>
        <Color x:Key="TextSecondary">#94A3B8</Color>
        <Color x:Key="TextMuted">#64748B</Color>
        <Color x:Key="AccentBlue">#60A5FA</Color>
        <Color x:Key="Success">#10B981</Color>
        <Color x:Key="Warning">#F59E0B</Color>
        <Color x:Key="Error">#EF4444</Color>

        <!-- Modern Gradients -->
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#334155" Offset="0"/>
            <GradientStop Color="#475569" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#475569" Offset="0"/>
            <GradientStop Color="#64748B" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonHoverGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#64748B" Offset="0"/>
            <GradientStop Color="#475569" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="SuccessGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#10B981" Offset="0"/>
            <GradientStop Color="#059669" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="WarningGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F59E0B" Offset="0"/>
            <GradientStop Color="#D97706" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ProgressGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#60A5FA" Offset="0"/>
            <GradientStop Color="#3B82F6" Offset="0.5"/>
            <GradientStop Color="#2563EB" Offset="1"/>
        </LinearGradientBrush>

        <!-- Storyboards for Animations -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Modern Button Style with Hover Effects -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{StaticResource ButtonGradient}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" 
                                                ShadowDepth="2" 
                                                Color="#000000" 
                                                Opacity="0.25"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonHoverGradient}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="White" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush">
                <Setter.Value>
                    <SolidColorBrush Color="White" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#60A5FA"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="White" Opacity="0.15"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="12" 
                                                        ShadowDepth="0" 
                                                        Color="#60A5FA" 
                                                        Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ProgressBar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="{StaticResource ProgressGradient}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="3"/>
                            <Border Name="PART_Track" CornerRadius="3" ClipToBounds="True">
                                <Border Name="PART_Indicator" 
                                        HorizontalAlignment="Left" 
                                        CornerRadius="3"
                                        Background="{TemplateBinding Foreground}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#3B82F6" 
                                                        BlurRadius="6" 
                                                        ShadowDepth="0" 
                                                        Opacity="0.5"/>
                                    </Border.Effect>
                                </Border>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Button Style -->
        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Status Badge Style -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section with Modern Design -->
        <Border Grid.Row="0" 
                Background="{StaticResource HeaderGradient}" 
                Padding="20,16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="16" 
                                ShadowDepth="3" 
                                Color="#000000" 
                                Opacity="0.2"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- App Title with Icon -->
                <StackPanel Grid.Row="0" 
                           Orientation="Horizontal" 
                           Margin="0,0,0,14">
                    
                    <Border Background="White" 
                            CornerRadius="8" 
                            Width="32" 
                            Height="32" 
                            Margin="0,0,12,0">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" 
                                            ShadowDepth="2" 
                                            Color="#3B82F6" 
                                            Opacity="0.4"/>
                        </Border.Effect>
                        <Viewbox Width="20" Height="20">
                            <Canvas Width="24" Height="24">
                                <Path Fill="#1E40AF" Data="M12,2 L2,7 L12,12 L22,7 Z"/>
                                <Path Stroke="#1E40AF" StrokeThickness="2" Data="M2,17 L12,22 L22,17"/>
                                <Path Stroke="#1E40AF" StrokeThickness="2" Data="M2,12 L12,17 L22,12"/>
                            </Canvas>
                        </Viewbox>
                    </Border>
                    
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="MyFastDownloader" 
                                 FontSize="20" 
                                 FontWeight="Bold" 
                                 Foreground="White"/>
                        <TextBlock Text="Modern Download Manager" 
                                 FontSize="11" 
                                 Foreground="#94A3B8" 
                                 Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- URL Input Section with Modern Design -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="UrlTextBox" 
                             Grid.Column="0"
                             Style="{StaticResource ModernTextBox}"
                             Text="{Binding DownloadUrl, UpdateSourceTrigger=PropertyChanged}"
                             KeyDown="UrlTextBox_KeyDown">
                    </TextBox>

                    <Button x:Name="AddButton" 
                            Grid.Column="1" 
                            Style="{StaticResource ModernButton}"
                            Background="{StaticResource SuccessGradient}"
                            Margin="12,0,0,0"
                            Padding="18,8"
                            Click="AddButton_Click"
                            ToolTip="Thêm download mới (Enter)">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" FontSize="16" FontWeight="Bold" Margin="0,0,8,0"/>
                            <TextBlock Text="Thêm Download" FontSize="13" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="2" 
                            Style="{StaticResource IconButton}"
                            Background="{StaticResource ButtonGradient}"
                            Margin="10,0,0,0"
                            ToolTip="Cài đặt"
                            Click="SettingsButton_Click">
                        <TextBlock Text="⚙" FontSize="14"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content Area with Enhanced Design -->
        <Border Grid.Row="1" 
                Margin="20" 
                Background="#1E293B" 
                CornerRadius="16" 
                BorderBrush="#334155" 
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" 
                                ShadowDepth="6" 
                                Color="#000000" 
                                Opacity="0.25"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Downloads Header with Stats -->
                <Grid Grid.Row="0" Margin="20,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="📥" 
                                 FontSize="20" 
                                 Margin="0,0,10,0" 
                                 VerticalAlignment="Center"/>
                        <StackPanel>
                            <TextBlock Text="Danh sách tải xuống" 
                                     FontSize="17" 
                                     FontWeight="SemiBold" 
                                     Foreground="#E2E8F0"/>
                            <TextBlock Text="{Binding DownloadStats, Mode=OneWay}" 
                                     FontSize="12" 
                                     Foreground="#94A3B8" 
                                     Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <Button Grid.Column="1" 
                            Style="{StaticResource ModernButton}"
                            Background="{StaticResource ButtonGradient}"
                            Padding="12,6"
                            ToolTip="Mở thư mục Downloads">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📂" FontSize="13" Margin="0,0,6,0"/>
                            <TextBlock Text="Mở thư mục" FontSize="12"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Downloads List with Modern Layout -->
                <Border Grid.Row="1" 
                        BorderBrush="#475569" 
                        BorderThickness="0,1,0,0"
                        CornerRadius="0,0,16,16"
                        ClipToBounds="True">
                    <Grid>
                        <!-- Downloads ListView -->
                        <ListView x:Name="DownloadsListView" 
                                  ItemsSource="{Binding Downloads}"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListView.Resources>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#334155"/>
                                    <Setter Property="Padding" Value="20,12"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border x:Name="border"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background">
                                                            <Setter.Value>
                                                                <SolidColorBrush Color="#3B82F6" Opacity="0.06"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>

                                <!-- Custom Scrollbar -->
                                <Style TargetType="ScrollBar">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Width" Value="6"/>
                                </Style>
                            </ListView.Resources>
                            
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2.5*"/>
                                            <ColumnDefinition Width="110"/>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="130"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- File Info Section -->
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FileName, Mode=OneWay}" 
                                                     FontWeight="Medium" 
                                                     FontSize="14" 
                                                     Foreground="#E2E8F0" 
                                                     TextTrimming="CharacterEllipsis"
                                                     ToolTip="{Binding FileName}"/>
                                            <TextBlock FontSize="12" 
                                                       Foreground="#94A3B8" 
                                                       Margin="0,4,0,0">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} / {1}">
                                                        <Binding Path="Downloaded" 
                                                                 Converter="{StaticResource BytesToStringConverter}" />
                                                        <Binding Path="TotalSize" 
                                                                 Converter="{StaticResource BytesToStringConverter}" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- Status Badge -->
                                        <Border Grid.Column="1" 
                                                Style="{StaticResource StatusBadge}"
                                                BorderBrush="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"
                                                VerticalAlignment="Center">
                                            <Border.Background>
                                                <SolidColorBrush Color="Black" Opacity="0.1"/>
                                            </Border.Background>
                                            <TextBlock Text="{Binding StatusText, Mode=OneWay}" 
                                                     Foreground="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"
                                                     FontSize="11" 
                                                     FontWeight="SemiBold"
                                                     TextAlignment="Center"/>
                                        </Border>

                                        <!-- Progress Section -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                            <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}" 
                                                       Maximum="100" 
                                                       Style="{StaticResource ModernProgressBar}"
                                                       Height="6"/>
                                            <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:F1}%, Mode=OneWay}" 
                                                     FontSize="12" 
                                                     Foreground="#94A3B8" 
                                                     TextAlignment="Center"
                                                     Margin="0,5,0,0"
                                                     FontWeight="Medium"/>
                                        </StackPanel>

                                        <!-- Speed Display -->
                                        <StackPanel Grid.Column="3" 
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center">
                                            <TextBlock Text="⚡" 
                                                     FontSize="14" 
                                                     HorizontalAlignment="Center"
                                                     Margin="0,0,0,3"/>
                                            <TextBlock Text="{Binding SpeedBytesPerSec, Converter={StaticResource SpeedToStringConverter}, Mode=OneWay}" 
                                                     FontSize="13" 
                                                     FontWeight="SemiBold"
                                                     Foreground="#60A5FA"
                                                     HorizontalAlignment="Center"/>
                                        </StackPanel>

                                        <!-- Action Buttons -->
                                        <StackPanel Grid.Column="4" 
                                                  Orientation="Horizontal" 
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center">
                                            <!-- Play/Pause/Resume Button -->
                                            <Button Padding="8"
                                                    Width="34"
                                                    Height="34"
                                                    Margin="0,0,8,0"
                                                    Click="ActionButton_Click"
                                                    Tag="{Binding}"
                                                    Cursor="Hand">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Background" Value="{StaticResource SuccessGradient}"/>
                                                        <Setter Property="Content" Value="▶"/>
                                                        <Setter Property="ToolTip" Value="Bắt đầu tải"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Downloading">
                                                                <Setter Property="Background" Value="{StaticResource WarningGradient}"/>
                                                                <Setter Property="Content" Value="⏸"/>
                                                                <Setter Property="ToolTip" Value="Tạm dừng"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Paused">
                                                                <Setter Property="Background" Value="{StaticResource SuccessGradient}"/>
                                                                <Setter Property="Content" Value="▶"/>
                                                                <Setter Property="ToolTip" Value="Tiếp tục"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                                                <Setter Property="Background" Value="{StaticResource SuccessGradient}"/>
                                                                <Setter Property="Content" Value="↻"/>
                                                                <Setter Property="ToolTip" Value="Thử lại"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Queued">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            
                                            <!-- Open Folder Button -->
                                            <Button Padding="8"
                                                    Width="34"
                                                    Height="34"
                                                    Click="OpenButton_Click"
                                                    Tag="{Binding}"
                                                    Cursor="Hand">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource IconButton}">
                                                        <Setter Property="Background" Value="{StaticResource ButtonGradient}"/>
                                                        <Setter Property="Content" Value="📂"/>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Setter Property="ToolTip" Value="Mở thư mục chứa file"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <!-- Empty State -->
                        <StackPanel VerticalAlignment="Center" 
                                    HorizontalAlignment="Center"
                                    Visibility="{Binding EmptyStateVisibility, Mode=OneWay}">
                            
                            <Viewbox Width="100" Height="100" Margin="0,0,0,20">
                                <Canvas Width="24" Height="24">
                                    <Path Fill="#475569" Opacity="0.5" Data="M5,20H19V18H5M19,9H15V3H9V9H5L12,16L19,9Z"/>
                                </Canvas>
                            </Viewbox>
                            
                            <TextBlock Text="Chưa có download nào" 
                                     FontSize="18" 
                                     FontWeight="SemiBold"
                                     Foreground="#64748B" 
                                     HorizontalAlignment="Center"/>
                            <TextBlock Text="Nhập URL ở trên để bắt đầu tải xuống" 
                                     FontSize="13" 
                                     Foreground="#64748B" 
                                     Margin="0,10,0,0" 
                                     HorizontalAlignment="Center"/>
                            <TextBlock Text="hoặc sử dụng bookmark để tải từ trình duyệt" 
                                     FontSize="12" 
                                     Foreground="#64748B" 
                                     Margin="0,3,0,0" 
                                     HorizontalAlignment="Center"
                                     Opacity="0.7"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Footer with Enhanced Design -->
        <Border Grid.Row="2" 
                Background="#1E293B" 
                BorderBrush="#334155" 
                BorderThickness="0,1,0,0" 
                Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Server Status -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#10B981" 
                            Width="6" 
                            Height="6" 
                            CornerRadius="3" 
                            Margin="0,0,8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#10B981" 
                                            BlurRadius="6" 
                                            ShadowDepth="0"/>
                        </Border.Effect>
                    </Border>
                    <TextBlock Text="Server đang chạy tại " 
                             FontSize="12" 
                             Foreground="#94A3B8"
                             VerticalAlignment="Center"/>
                    <TextBlock Text="http://localhost:4153" 
                             FontSize="12" 
                             Foreground="#60A5FA" 
                             FontWeight="Medium"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Status Message -->
                <TextBlock Grid.Column="1"
                         Text="{Binding StatusMessage, Mode=OneWay}" 
                         FontSize="12" 
                         Foreground="#60A5FA"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         FontWeight="Medium"/>

                <!-- Copy Bookmark Button -->
                <Button Grid.Column="2"
                        Style="{StaticResource ModernButton}"
                        Background="{StaticResource ButtonGradient}"
                        Padding="14,6"
                        Click="CopyBookmarkButton_Click"
                        ToolTip="Sao chép bookmark để thêm vào trình duyệt">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="13" Margin="0,0,6,0"/>
                        <TextBlock Text="Copy Bookmark" FontSize="12" FontWeight="Medium"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
